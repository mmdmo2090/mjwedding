# These are auto-injected by Vercel's Neon Postgres integration in
# deployed environments (Production/Preview). For local development,
# run `vercel env pull .env.local` after connecting the database to
# the "Development" environment in the Vercel dashboard.
MJWEDDING_DATABASE_URL="postgresql://user:password@localhost:5432/mjwedding?schema=public"
MJWEDDING_POSTGRES_URL_NON_POOLING="postgresql://user:password@localhost:5432/mjwedding?schema=public"

ADMIN_PASSWORD_HASH=""
